{
  "name": "jsonld-graph",
  "version": "0.1.0",
  "description": "jsonld-graph",
  "main": "docs/index.min.js",
  "scripts": {
    "build": "npm run rollup",
    "rollup": "rollup -c rollup.config.mjs",
    "watch": "rollup -w -c rollup.config.mjs"
  },
  "dependencies": {
    "d3": "^7.8.5",
    "jsonld": "^8.3.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.23.2",
    "@rollup/plugin-babel": "^6.0.4",
    "rollup": "^4.3.0"
  }
}
